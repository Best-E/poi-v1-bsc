# POI v1: @usernames for humans, #agentIDs for machines

Stop clipboard hijacking on BSC for $0.01 gas. Address-first security with optional human/agent checksum.

**Contract**: Deploy on BSC. Fees in BNB. MIT License.

**Core Rule**: All transfers use 0xAddress. If @name or #agentID mismatches, hard block.

**Deploy**: See /contracts. Run `npm run deploy:bscTestnet`.
